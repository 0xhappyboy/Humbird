mod http {
    // application launcher
    #[proc_macro_attribute]
    pub fn boot(attr: TokenStream, item: TokenStream) -> TokenStream {
        // TODO
        item
    }
    //process processing of get request method based on http protocol
    #[proc_macro_attribute]
    pub fn get_mapping(attr: TokenStream, item: TokenStream) -> TokenStream {
        // TODO
        item
    }
    //process processing of post request method based on http protocol
    #[proc_macro_attribute]
    pub fn post_mapping(attr: TokenStream, item: TokenStream) -> TokenStream {
        // TODO
        item
    }
    //process processing of put request method based on http protocol
    #[proc_macro_attribute]
    pub fn put_mapping(attr: TokenStream, item: TokenStream) -> TokenStream {
        // TODO
        item
    }
    //process processing of head request method based on http protocol
    #[proc_macro_attribute]
    pub fn head_mapping(attr: TokenStream, item: TokenStream) -> TokenStream {
        // TODO
        item
    }
    //process processing of delete request method based on http protocol
    #[proc_macro_attribute]
    pub fn delete_mapping(attr: TokenStream, item: TokenStream) -> TokenStream {
        // TODO
        item
    }
    //process processing of connect request method based on http protocol
    #[proc_macro_attribute]
    pub fn connect_mapping(attr: TokenStream, item: TokenStream) -> TokenStream {
        // TODO
        item
    }
    //process processing of options request method based on http protocol
    #[proc_macro_attribute]
    pub fn options_mapping(attr: TokenStream, item: TokenStream) -> TokenStream {
        // TODO
        item
    }
    //process processing of trace request method based on http protocol
    #[proc_macro_attribute]
    pub fn trace_mapping(attr: TokenStream, item: TokenStream) -> TokenStream {
        // TODO
        item
    }
}
